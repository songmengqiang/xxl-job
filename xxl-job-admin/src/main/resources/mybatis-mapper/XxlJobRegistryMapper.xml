<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="XxlJobRegistryMapper">
	
	<resultMap id="XxlJobRegistry" type="com.xxl.job.admin.core.model.XxlJobRegistry" >
		<result column="id" property="id" jdbcType = "INTEGER" />
	    <result column="registry_group" property="registryGroup" jdbcType = "VARCHAR"  />
	    <result column="registry_key" property="registryKey" jdbcType = "VARCHAR"  />
	    <result column="registry_value" property="registryValue" jdbcType = "VARCHAR"  />
		<result column="update_time" property="updateTime" jdbcType = "TIMESTAMP"  />
	</resultMap>

	<sql id="Base_Column_List">
		t.id,
		t.registry_group,
		t.registry_key,
		t.registry_value,
		t.update_time
	</sql>
	
	<delete id="removeDead" parameterType="java.lang.Integer" >
		DELETE FROM XXL_JOB_QRTZ_TRIGGER_REGISTRY
		WHERE update_time <![CDATA[ < ]]> sysdate-#{timeout,jdbcType=INTEGER}/(1*24*60*60)
	</delete>

	<select id="findAll" parameterType="java.lang.Integer" resultMap="XxlJobRegistry">
		SELECT <include refid="Base_Column_List" />
		FROM XXL_JOB_QRTZ_TRIGGER_REGISTRY t
		WHERE t.update_time <![CDATA[ > ]]> sysdate-#{timeout,jdbcType=INTEGER}/(1*24*60*60)
	</select>

    <update id="registryUpdate" parameterType="java.util.Map" >
        UPDATE XXL_JOB_QRTZ_TRIGGER_REGISTRY
        SET update_time = sysdate
        WHERE registry_group = #{registryGroup,jdbcType=VARCHAR}
          AND registry_key = #{registryKey,jdbcType=VARCHAR}
          AND registry_value = #{registryValue,jdbcType=VARCHAR}
    </update>

    <insert id="registrySave" parameterType="java.util.Map" useGeneratedKeys="true" keyProperty="id" >
	    <selectKey resultType="int" order="BEFORE" keyProperty="id">  
	         SELECT SEQ_XXL_JOB_QRTZ_TRIGGER_REGIS.NEXTVAL FROM dual  
	    </selectKey>
        INSERT INTO XXL_JOB_QRTZ_TRIGGER_REGISTRY(id, registry_group , registry_key , registry_value, update_time)
        VALUES(#{id,jdbcType=INTEGER},
          #{registryGroup,jdbcType=VARCHAR}, #{registryKey,jdbcType=VARCHAR} , #{registryValue,jdbcType=VARCHAR}, sysdate)
    </insert>

</mapper>